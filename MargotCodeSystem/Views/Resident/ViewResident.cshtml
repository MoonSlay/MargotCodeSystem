@* @model MargotCodeSystem.Database.DbModels.ResidentViewModel

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Resident Details</title>
    <style>


        .details-container {
            margin: 20px;
        }

            .details-container h2 {
                font-size: 24px;
                margin-bottom: 20px;
            }

            .details-container table {
                width: 100%;
                border-collapse: collapse;
                margin-bottom: 20px;
                margin-top: 10px;
            }

                .details-container table th,
                .details-container table td {
                    padding: 10px;
                    border: 1px solid black;
                }

                .details-container table th {
                    background-color: lightcoral;
                }

        .title {
            font-family: 'OPTIFrankfurter';
            font-size: 30px;
            color: #C21A22;
            margin-top: 130px;
        }

        .back-button {
            padding: 10px 20px;
            font-family: 'OPTIFrankfurter';
            background-color: #F4B628;
            color: white;
            text-decoration: none;
            border-radius: 10px;
        }

            .back-button:hover {
                background-color: #CF9207;
            }

        .space {
            margin-top: 20px;
        }

        th, td {
            text-align: left;
            font-family: 'OPTIFrankfurter';
        }

        h1 {
            font-family: 'OPTIFrankfurter';
            font-size: 40px;
            color: #C21A22;
            padding-left: 25px;
            padding-top: 5px;
        }

        .top-bar {
            background-color: #F4B628;
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            border-radius: 0 0 30px 30px;
        }

    </style>
</head>

<body>
    <div class="top-bar">
        <h1> Resident Details: @Model.Resident.Fullname</h1>
    </div>

    <div class="details-container">
        <table class="space">
            <tr>
                <th>Full Name</th>
                <td>@Model.Resident.Fullname</td>
            </tr>
            <!-- Add other resident details here -->
            @if (Model.HouseoccupantGroup == null)
            {
                <tr>
                    <td>No house occupants found.</td>
                </tr>
            }
            else
            {
                <tr>
                    <th colspan="6">House Occupants</th>
                </tr>
                            @foreach (var occupant in group.HouseOccupants)
                            {
                                <tr>
                                    <td>@occupant.FullName</td>
                                    <td>@occupant.Position</td>
                                    <td>@occupant.Age</td>
                                    <td>@occupant.BirthDate</td>
                                    <td>@occupant.CivilStatus</td>
                                    <td>@occupant.SourceIncome</td>
                                </tr>
                            }
                }
            }
        </table>

        <a href="@Url.Action("Dashboard", "Resident")" class="back-button">BACK TO DASHBOARD</a>
        <a href="@Url.Action("PrintDetails", "Resident", new { id = Model.Resident.Id })" target="_blank" class="back-button">PRINT FILE</a>
    </div>
</body>
</html>
 *@